# Zephyr-80 CP/M 2.2 Memory Map

Generated by `tools/generate_memory_docs.py` from `build/firmware.lst`, `build/layout.manifest`, and image artifacts.

## Address Space Overview

| Range | Use | Notes |
|---|---|---|
| `0000h-00FFh` | Page zero and default DMA area | Runtime code installs `JP WBOOT` at `0000h` and `JP FBASE` at `0005h`; default DMA/command tail starts at `0080h`. |
| `0100h-BFFFh` | CP/M banked TPA | Runnable programs may use this banked low-memory range. |
| `C000h-C3FFh` | Protected/common TPA | Application-owned common TPA. The marker range is `C000h-C3FFh`; the effective TPA portion stops before `CBASE`. |
| `C400h-CBABh` | CP/M CCP | `CBASE` is `C400h`. |
| `CC06h-D9FFh` | CP/M BDOS and state | `FBASE` is `CC06h` in the current assembled image. |
| `DA00h-DDFFh` | Core BIOS | BIOS jump table, BOOT/WBOOT, page-zero setup, console facade, storage facade, banking, XMOVE, and LAUNCH. |
| `DE00h-E1FFh` | Driver slot 0 | Current transitional owner: RAM disk backend. |
| `E200h-E5FFh` | Driver slot 1 | Current transitional owner: legacy SIO console backend, including SIO IM2 trampoline/table. |
| `E600h-F5FFh` | Driver slots 2-5 | Available fixed 1 KiB slots for future drivers. |
| `F600h-F7FFh` | Protected BIOS scratch buffers | `MOVE_BUFFER` is at `F600h`; `RAMDISK_DIRBUF` is at `F700h`. |
| `F900h-F999h` | BIOS runtime state | Current bank, DMA address, console driver state, storage state, and banking state. |
| `FA00h-FFFFh` | Protected firmware stack and work window | Stack top is `FFF0h`; stack guard is `FA00h`. |

## Core BIOS Layout

| Range | Component |
|---|---|
| `0000h-0002h` | Reset vector: `JP cpm_rom_entry_high`. |
| `C400h-CBABh` | CP/M CCP area through `CCPSTACK`. |
| `CC06h-D9FFh` | CP/M BDOS, BDOS work variables, and CP/M tables. |
| `DA00h-DA41h` | Standard BIOS jump table plus `ZBIOS_EXT_BASE`. |
| `DA42h-DA44h` | `bdos_entry_shim`: compatibility jump to `FBASE`. |
| `DA45h-DA98h` | ROM-to-RAM shadow-copy boot code. |
| `DA99h-DAA6h` | Low-level bank selection helpers. |
| `DAA7h-DACAh` | SIO channel B initialization. |
| `DACBh-DB7Fh` | Cold boot, warm boot, CCP restore, page-zero, DMA, CTC helpers, and alignment gap. |
| `DB80h-DBBFh` | Console BIOS facade. |
| `DC00h-DC30h` | Storage BIOS facade. |
| `DC80h-DD84h` | Banking and high-memory `LAUNCH` implementation. |

## Driver Slot Table

| Slot | Start | End | Size | Owner | Current contents |
|---:|---:|---:|---:|---|---|
| 0 | `DE00h` | `E1FFh` | 1024 bytes | RAM disk backend | `DE00h-DF0Fh` RAM disk backend. |
| 1 | `E200h` | `E5FFh` | 1024 bytes | legacy SIO console backend | `E200h-E4E6h` legacy SIO code/trampoline; `E500h-E5FFh` SIO IM2 table. |
| 2 | `E600h` | `E9FFh` | 1024 bytes | available | Available. |
| 3 | `EA00h` | `EDFFh` | 1024 bytes | available | Available. |
| 4 | `EE00h` | `F1FFh` | 1024 bytes | available | Available. |
| 5 | `F200h` | `F5FFh` | 1024 bytes | available | Available. |

## BIOS Jump Table Layout

| Address | Entry |
|---:|---|
| `DA00h` | `JP boot` |
| `DA03h` | `JP wboot` |
| `DA06h` | `JP const` |
| `DA09h` | `JP conin` |
| `DA0Ch` | `JP conout` |
| `DA0Fh` | `JP list` |
| `DA12h` | `JP punch` |
| `DA15h` | `JP reader` |
| `DA18h` | `JP home` |
| `DA1Bh` | `JP seldsk` |
| `DA1Eh` | `JP settrk` |
| `DA21h` | `JP setsec` |
| `DA24h` | `JP setdma` |
| `DA27h` | `JP read` |
| `DA2Ah` | `JP write` |
| `DA2Dh` | `JP listst` |
| `DA30h` | `JP sectran` |
| `DA33h` | `JP MOVE` |
| `DA36h` | `JP XMOVE` |
| `DA39h` | `JP SELMEM` |
| `DA3Ch` | `JP SETBNK` |
| `DA3Fh` | `JP LAUNCH` |

## Runtime State Layout

| Range | State |
|---|---|
| `F900h` | `CURRENT_BANK` |
| `F901h-F902h` | `cbios_dma_addr` |
| `F904h-F905h` | `CONSOLE_DRIVER` |
| `F920h` | `SAVED_BANK` |
| `F921h` | `DMA_BANK` |
| `F922h` | `XMOVE_SRC_BANK` |
| `F923h` | `XMOVE_DST_BANK` |
| `F924h` | `XMOVE_PENDING` |
| `F925h` | `APP_LAUNCH_BANK` |
| `F926h-F927h` | `MOVE_SRC_PTR` |
| `F928h-F929h` | `MOVE_DST_PTR` |
| `F92Ah-F92Bh` | `MOVE_REMAIN` |
| `F92Ch-F92Dh` | `MOVE_CHUNK_LEN` |
| `F940h` | `ramdisk_selected_drive` |
| `F941h-F942h` | `ramdisk_track` |
| `F943h-F944h` | `ramdisk_sector` |
| `F949h-F958h` | `RAMDISK_CSV` |
| `F959h-F96Ah` | `RAMDISK_ALV` |
| `F970h` | `CONSOLE_RX_HEAD` |
| `F971h` | `CONSOLE_RX_TAIL` |
| `F972h` | `CONSOLE_RX_COUNT` |
| `F973h` | `CONSOLE_TX_HEAD` |
| `F974h` | `CONSOLE_TX_TAIL` |
| `F975h` | `CONSOLE_TX_COUNT` |
| `F976h` | `CONSOLE_TX_ACTIVE` |
| `F977h` | `CONSOLE_IRQ_ENABLED` |
| `F978h-F979h` | `CONSOLE_IRQ_COUNT` |
| `F97Ah-F989h` | `CONSOLE_RX_BUFFER` |
| `F98Ah-F999h` | `CONSOLE_TX_BUFFER` |

## Scratch Layout

| Range | Use | Notes |
|---|---|---|
| `F600h-F6FFh` | `MOVE_BUFFER` | Cross-bank MOVE and RAM disk transfer staging buffer. |
| `F700h-F77Fh` | `RAMDISK_DIRBUF` | CP/M directory buffer referenced by the RAM disk DPH. |
| `F780h-F7FFh` | unused scratch window | Derived from active buffers and scratch bounds. |

## IM2 Layout

| Field | Value | Notes |
|---|---:|---|
| Table base | `E500h` | Start of the repeated-byte table. |
| Table range | `E500h-E5FFh` | Exactly 256 bytes; `CONSOLE_IM2_VECTOR_TABLE_END` is the exclusive end label. |
| Vector page | `E5h` | Loaded into the Z80 I register. |
| Vector byte | `E4h` | Repeated table byte; E4h/E4h forms the trampoline address. |
| SIO WR2 vector byte | `00h` | Current SIO channel B vector byte. |
| Trampoline address | `E4E4h` | Contains `JP sio_console_isr`. |
| Owner | legacy SIO console backend | Transitional SIO-only build; IM2 table lives in driver slot 1. |

Future real video-card or storage interrupts may require global IM2 handling or a 257-byte FF-safe table. Devices that emit FFh vectors cannot rely on this compact SIO-only table without revisiting ownership and slot sizing.

## Image Payload Layout

| Payload | Bank | Entry | Size | End | Source |
|---|---:|---:|---:|---:|---|
| Monitor | 0 | `0100h` | 32512 | `8000h` | `images/zephyr80_monitor.bin` |
| BBC BASIC | 1 | `0100h` | 18944 | `4B00h` | `images/BBCBASIC.COM` |

## RAM Disk Layout

| Name | Banks | Range per Bank | Image Size | Capacity | Pad | Fill | Source |
|---|---:|---:|---:|---:|---:|---:|---|
| CP/M Drive A | 2-7 | `0000h-BFFFh` | 233472 | 294912 | 61440 | `E5h` | `images/zephyr80.cpm` |

## Image Artifacts

| Artifact | Size | Notes |
|---|---:|---|
| `build/firmware.bin` | 65536 bytes | Firmware image before payload attachment. |
| `build/zephyr80.pre-swap.bin` | 524288 bytes | Logical image after payload attachment and before CPU-board bit swap. |
| `build/zephyr80.bin` | 524288 bytes | Final burnable image after `tools/swapbits.py`. |

## Validation Report

Status: PASS. No fatal layout errors were found.

Validated expectations:

- BIOS core must stay inside CBIOS_CORE_BASE-CBIOS_CORE_END.
- Each declared driver must stay inside its declared fixed slot range.
- RAM disk backend must stay inside slot 0.
- Legacy SIO console backend and its IM2 table must stay inside slot 1.
- Scratch buffers must not overlap resident code.
- Runtime state must not overlap scratch, stack, or the SIO-owned IM2 table.
- Stack guard must remain above runtime state.
- Protected/common TPA C000h-C3FFh is application-owned and must not be used by BIOS.

Additional notes:

- CP/M drive A is backed by RAM banks 2-7 across `0000h-BFFFh` in each bank.
- The RAM disk seed image is copied from ROM to RAM during boot shadow-copy, then writes mutate RAM only.
- The protected TPA marker is `C000h-C3FFh`; for this build, the application-usable protected TPA subrange is `C000h-C3FFh` because `CBASE` starts at `C400h`.
- WBOOT restores the CCP range `C400h-CC05h` from ROM page 0 using `ROM_VISIBLE_BANK0` (`00h`) before returning to `CCP_CLEARBUF_ENTRY`.
- `CBIOS_BASE` is `DA00h`; CBIOS layout constants are derived from this base.
- `CBIOS_CODE_LIMIT` is `F600h`; no resident code may cross into scratch/staging.
- `LAUNCH` code resides at `DD2Eh`, inside protected high BIOS memory.
- `WBOOT` resident code starts at `DAFDh`, inside protected high BIOS memory.
- `ZBIOS_EXT_BASE` is at `DA33h` and exposes `MOVE`, `XMOVE`, `SELMEM`, `SETBNK`, and `LAUNCH`.
- The final bit-swapped image is `build/zephyr80.bin`.
