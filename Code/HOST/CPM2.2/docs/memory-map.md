# Zephyr-80 CP/M 2.2 Memory Map

Generated by `tools/generate_memory_docs.py` from `build/firmware.lst`, `build/layout.manifest`, and image artifacts.

## Address Space Overview

| Range | Use | Notes |
|---|---|---|
| `0000h-00FFh` | Page zero and default DMA area | Runtime code installs `JP WBOOT` at `0000h` and `JP FBASE` at `0005h`; default DMA/command tail starts at `0080h`. |
| `0100h-BFFFh` | CP/M banked TPA | Runnable programs may use this banked low-memory range. |
| `C000h-C3FFh` | Protected/common TPA | Application-owned common TPA. The marker range is `C000h-C3FFh`; the effective TPA portion stops before `CBASE`. |
| `C400h-CBABh` | CP/M CCP | `CBASE` is `C400h`. |
| `CC06h-D9FFh` | CP/M BDOS and state | `FBASE` is `CC06h` in the current assembled image. |
| `DA00h-DFFFh` | Core BIOS | BIOS jump table, BOOT/WBOOT, page-zero setup, console facade, storage facade, banking, XMOVE, LAUNCH, SIO core, and IOCALL transport. |
| `E000h-F3FFh` | Driver slots 0-4 | Virtual Drip console driver (code, font, shadow buffer, queues, state). |
| `F570h-F96Fh` | Driver slot 5 | Current transitional owner: RAM disk backend. |
| `F970h-FB6Fh` | Protected BIOS scratch buffers | `MOVE_BUFFER` is at `F970h`; `RAMDISK_DIRBUF` is at `FA70h`. |
| `FB70h-FBE8h` | BIOS runtime state | Current bank, DMA address, banking state, storage state, SIO core state, and console driver state. |
| `FC70h-FFFFh` | Protected firmware stack and work window | Stack top is `FFF0h`; console backend stack top is `FEF0h`; stack guard is `FC70h`. |

## Core BIOS Layout

| Range | Component |
|---|---|
| `0000h-0002h` | Reset vector: `JP cpm_rom_entry_high`. |
| `C400h-CBABh` | CP/M CCP area through `CCPSTACK`. |
| `CC06h-D9FFh` | CP/M BDOS, BDOS work variables, and CP/M tables. |
| `DA00h-DA44h` | Standard BIOS jump table plus `ZBIOS_EXT_BASE`. |
| `DA45h-DA47h` | `bdos_entry_shim`: compatibility jump to `FBASE`. |
| `DA48h-DA9Bh` | ROM-to-RAM shadow-copy boot code. |
| `DA9Ch-DAA9h` | Low-level bank selection helpers. |
| `DAAAh-DB78h` | Cold boot, warm boot, CCP restore, page-zero, DMA, CTC helpers, and alignment gap. |
| `DB79h-DBBFh` | Console BIOS facade. |
| `DBC0h-DBF0h` | Storage BIOS facade. |
| `DC00h-DD04h` | Banking and high-memory `LAUNCH` implementation. |
| `DD10h-DF4Fh` | SIO core and exact IM2 vector entry. |
| `DF50h-DFE5h` | IOCALL transaction transport. |

## Driver Slot Table

| Slot | Start | End | Size | Owner | Current contents |
|---:|---:|---:|---:|---|---|
| 0 | `E000h` | `E3FFh` | 1024 bytes | Virtual Drip console driver | `E000h-F56Ch` VDrip code, font, shadow, queues, state. |
| 1 | `E400h` | `E7FFh` | 1024 bytes | Virtual Drip console driver | Part of Virtual Drip console driver above. |
| 2 | `E800h` | `EBFFh` | 1024 bytes | Virtual Drip console driver | Part of Virtual Drip console driver above. |
| 3 | `EC00h` | `EFFFh` | 1024 bytes | Virtual Drip console driver | Part of Virtual Drip console driver above. |
| 4 | `F000h` | `F3FFh` | 1024 bytes | Virtual Drip console driver | Part of Virtual Drip console driver above. |
| 5 | `F570h` | `F96Fh` | 1024 bytes | RAM disk backend | `F570h-F67Fh` RAM disk backend. |

## Core SIO Layout

| Range | Owner | Notes |
|---|---|---|
| `DD10h-DF4Fh` | SIO core | BIOS-owned SIO0/B async setup with CTS-polled TX and software-managed RTS helpers, SIO1/A sync setup, SIO IRQ control, RX sink registration, byte I/O APIs, IO Controller RTS helpers, RX diagnostics, RX kick, ISR, and compatibility labels. |
| `DD10h-DD11h` | SIO core | Exact two-byte IM2 vector table entry. |

SIO_CH_CONSOLE is the BIOS-owned SIO0/B async console link used by the Virtual Drip console driver. It uses RTS/CTS hardware flow control without requiring DTR/DCD. WR3 Auto Enables remain off intentionally to avoid DCD dependence. The VDrip console driver manages RTS through its own high/low watermark logic for host-to-Zephyr backpressure and VDP traffic gating.

## VDrip Console Layout

| Range | Owner | Notes |
|---|---|---|
| `E000h-F56Ch` | Virtual Drip console driver | CP/M console semantics, Virtual Drip packet transport, VDP text rendering, font, shadow buffer, RX ring, input queue, and cursor/viewport state. |

SIO_CH_IOCTRL is the BIOS-owned SIO1/A synchronous IO Controller link. SIO1/A uses external clock and external sync from the MCU, with RTS as the service-request signal. SIO1/A interrupts are disabled in this build.
`IOCALL` is core BIOS code at `DF50h-DFE5h` and does not occupy driver slot space.

## BIOS Jump Table Layout

| Address | Entry |
|---:|---|
| `DA00h` | `JP boot` |
| `DA03h` | `JP wboot` |
| `DA06h` | `JP const` |
| `DA09h` | `JP conin` |
| `DA0Ch` | `JP conout` |
| `DA0Fh` | `JP list` |
| `DA12h` | `JP punch` |
| `DA15h` | `JP reader` |
| `DA18h` | `JP home` |
| `DA1Bh` | `JP seldsk` |
| `DA1Eh` | `JP settrk` |
| `DA21h` | `JP setsec` |
| `DA24h` | `JP setdma` |
| `DA27h` | `JP read` |
| `DA2Ah` | `JP write` |
| `DA2Dh` | `JP listst` |
| `DA30h` | `JP sectran` |
| `DA33h` | `JP MOVE` |
| `DA36h` | `JP XMOVE` |
| `DA39h` | `JP SELMEM` |
| `DA3Ch` | `JP SETBNK` |
| `DA3Fh` | `JP LAUNCH` |
| `DA42h` | `JP IOCALL` |

## Runtime State Layout

| Range | State |
|---|---|
| `FB70h` | `CURRENT_BANK` |
| `FB71h-FB72h` | `cbios_dma_addr` |
| `FB74h-FB75h` | `CONSOLE_DRIVER` |
| `FB76h-FB77h` | `CONSOLE_CALLER_SP` |
| `FB90h` | `SAVED_BANK` |
| `FB91h` | `DMA_BANK` |
| `FB92h` | `XMOVE_SRC_BANK` |
| `FB93h` | `XMOVE_DST_BANK` |
| `FB94h` | `XMOVE_PENDING` |
| `FB95h` | `APP_LAUNCH_BANK` |
| `FB96h-FB97h` | `MOVE_SRC_PTR` |
| `FB98h-FB99h` | `MOVE_DST_PTR` |
| `FB9Ah-FB9Bh` | `MOVE_REMAIN` |
| `FB9Ch-FB9Dh` | `MOVE_CHUNK_LEN` |
| `FBB0h` | `ramdisk_selected_drive` |
| `FBB1h-FBB2h` | `ramdisk_track` |
| `FBB3h-FBB4h` | `ramdisk_sector` |
| `FBB9h-FBC8h` | `RAMDISK_CSV` |
| `FBC9h-FBDAh` | `RAMDISK_ALV` |
| `FBE0h-FBE1h` | `SIO0B_RX_SINK` |
| `FBE2h-FBE3h` | `SIO1_RX_SINK` |
| `FBE4h` | `SIO_CORE_IRQ_ENABLED` |
| `FBE5h-FBE6h` | `SIO_CORE_IRQ_COUNT` |
| `FBE7h` | `SIO0B_LAST_RR1` |
| `FBE8h` | `SIO0B_LAST_RX_ERROR` |

## Scratch Layout

| Range | Use | Notes |
|---|---|---|
| `F970h-FA6Fh` | `MOVE_BUFFER` | Cross-bank MOVE and RAM disk transfer staging buffer. |
| `FA70h-FAEFh` | `RAMDISK_DIRBUF` | CP/M directory buffer referenced by the RAM disk DPH. |
| `FAF0h-FB6Fh` | unused scratch window | Derived from active buffers and scratch bounds. |

## IM2 Layout

| Field | Value | Notes |
|---|---:|---|
| Table base | `DD10h` | Start of the exact IM2 vector table entry. |
| Table range | `DD10h-DD11h` | Exactly 2 bytes; `CONSOLE_IM2_VECTOR_TABLE_END` is the exclusive end label. |
| Vector page | `DDh` | Loaded into the Z80 I register. |
| SIO0/B WR2 vector byte | `10h` | Selects the exact two-byte table entry. |
| Entry address | `DD10h` | Contains the little-endian word `sio_core_isr`. |
| Owner | SIO core | The IM2 entry lives inside core BIOS with the SIO core. |

Future devices that need additional IM2 vectors should allocate explicit table entries and program their vector bytes directly; this build no longer emits a 256-byte repeated table.

## Image Payload Layout

| Payload | Bank | Entry | Size | End | Source |
|---|---:|---:|---:|---:|---|
| Monitor | 0 | `0100h` | 32512 | `8000h` | `images/zephyr80_monitor.bin` |
| BBC BASIC | 1 | `0100h` | 18944 | `4B00h` | `images/BBCBASIC.COM` |

## RAM Disk Layout

| Name | Banks | Range per Bank | Image Size | Capacity | Pad | Fill | Source |
|---|---:|---:|---:|---:|---:|---:|---|
| CP/M Drive A | 2-7 | `0000h-BFFFh` | 233472 | 294912 | 61440 | `E5h` | `images/zephyr80.cpm` |

## Image Artifacts

| Artifact | Size | Notes |
|---|---:|---|
| `build/firmware.bin` | 65536 bytes | Firmware image before payload attachment. |
| `build/zephyr80.pre-swap.bin` | 524288 bytes | Logical image after payload attachment and before CPU-board bit swap. |
| `build/zephyr80.bin` | 524288 bytes | Final burnable image after `tools/swapbits.py`. |

## Validation Report

Status: PASS. No fatal layout errors were found.

Validated expectations:

- BIOS core must stay inside CBIOS_CORE_BASE-CBIOS_CORE_END.
- Core BIOS component ranges must not overlap.
- Each declared driver must stay inside its declared fixed slot range.
- Virtual Drip console driver must stay inside slots 0-5.
- RAM disk backend must stay inside slot 5.
- SIO core and its exact IM2 vector entry must stay inside core BIOS.
- Scratch buffers must not overlap resident code.
- Runtime state must not overlap scratch, stack, or the SIO-owned IM2 table.
- Stack guard must remain above runtime state.
- Protected/common TPA C000h-C3FFh is application-owned and must not be used by BIOS.

Additional notes:

- CP/M drive A is backed by RAM banks 2-7 across `0000h-BFFFh` in each bank.
- The RAM disk seed image is copied from ROM to RAM during boot shadow-copy, then writes mutate RAM only.
- The protected TPA marker is `C000h-C3FFh`; for this build, the application-usable protected TPA subrange is `C000h-C3FFh` because `CBASE` starts at `C400h`.
- WBOOT restores the CCP range `C400h-CC05h` from ROM page 0 using `ROM_VISIBLE_BANK0` (`00h`) before returning to `CCP_CLEARBUF_ENTRY`.
- `CBIOS_BASE` is `DA00h`; CBIOS layout constants are derived from this base.
- `CBIOS_CODE_LIMIT` is `F970h`; no resident code may cross into scratch/staging.
- SIO core code starts at `DD10h` inside core BIOS; the Virtual Drip console driver starts at `E000h` spanning slots 0-5.
- `IOCALL` code starts at `DF50h` inside core BIOS and uses the BIOS-owned SIO1/A synchronous IO Controller transport.
- `LAUNCH` code resides at `DCAEh`, inside protected high BIOS memory.
- `WBOOT` resident code starts at `DADCh`, inside protected high BIOS memory.
- `ZBIOS_EXT_BASE` is at `DA33h` and exposes `MOVE`, `XMOVE`, `SELMEM`, `SETBNK`, `LAUNCH`, and `IOCALL`.
- The final bit-swapped image is `build/zephyr80.bin`.
