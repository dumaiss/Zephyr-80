# Zephyr-80 CP/M 2.2 Memory Map

Generated by `tools/generate_memory_docs.py` from `build/firmware.lst`, `build/layout.manifest`, and image artifacts.

## Address Space Overview

| Range | Use | Notes |
|---|---|---|
| `0000h-00FFh` | Page zero and default DMA area | Runtime code installs `JP WBOOT` at `0000h` and `JP FBASE` at `0005h`; default DMA/command tail starts at `0080h`. |
| `0100h-BFFFh` | CP/M banked TPA | Runnable programs may use this banked low-memory range. |
| `C000h-C3FFh` | Protected/common TPA | Application-owned common TPA. The marker range is `C000h-C3FFh`; the effective TPA portion stops before `CBASE`. |
| `C400h-CBABh` | CP/M CCP | `CBASE` is `C400h`. |
| `CC06h-D9FFh` | CP/M BDOS and state | `FBASE` is `CC06h` in the current assembled image. |
| `DA00h-DFFFh` | Core BIOS | BIOS jump table, BOOT/WBOOT, page-zero setup, console facade, storage facade, banking, XMOVE, SIO core, VIDEO_SEND extension, and IOCALL transport. |
| `E000h-F3FFh` | Driver slots 0-4 | Virtual Drip V9958 console driver (code, printable-run buffer, input queue, parser, and cursor state). |
| `F680h-FA7Fh` | Driver slot 5 | VDrip storage backend. |
| `FA80h-FDFFh` | Protected BIOS scratch/storage buffers | `MOVE_BUFFER` is at `FA80h`; `VDRIP_STORAGE_DPHDPB` is at `FB40h`; `VDRIP_STORAGE_DIRBUF` is at `FB80h`; `VDRIP_STORAGE_ALV` is at `FC00h`. |
| `FE00h-FE78h` | BIOS runtime state | Current bank, DMA address, banking state, storage state, SIO core state, and console driver state. |
| `FE80h-FFFFh` | Protected firmware stack and work window | Stack top is `FFF0h`; console backend stack top is `FF80h`; stack guard is `FE80h`. |

## Core BIOS Layout

| Range | Component |
|---|---|
| `0000h-0002h` | Reset vector: `JP cpm_rom_entry_high`. |
| `C400h-CBABh` | CP/M CCP area through `CCPSTACK`. |
| `CC06h-D9FFh` | CP/M BDOS, BDOS work variables, and CP/M tables. |
| `DA00h-DA44h` | Standard BIOS jump table plus `ZBIOS_EXT_BASE`. |
| `DA45h-DA47h` | `bdos_entry_shim`: compatibility jump to `FBASE`. |
| `DA48h-DA9Bh` | ROM-to-RAM shadow-copy boot code. |
| `DA9Ch-DAA9h` | Low-level bank selection helpers. |
| `DAAAh-DB78h` | Cold boot, warm boot, CCP restore, page-zero, DMA, CTC helpers, and alignment gap. |
| `DB79h-DBBFh` | Console BIOS facade. |
| `DBC0h-DBFEh` | Storage BIOS facade. |
| `DC00h-DCADh` | Banking and XMOVE implementation. |
| `DD10h-DF4Fh` | SIO core and exact IM2 vector entry. |
| `DF50h-DF77h` | BIOS extension: `VIDEO_SEND`. |
| `DF7Bh-DFC7h` | IOCALL IO Controller transport. |

## Driver Slot Table

| Slot | Start | End | Size | Owner | Current contents |
|---:|---:|---:|---:|---|---|
| 0 | `E000h` | `E3FFh` | 1024 bytes | Virtual Drip console driver | `E000h-EC9Fh` VDrip V9958 code, run/input queues, parser, and sprite-cursor state. |
| 1 | `E400h` | `E7FFh` | 1024 bytes | Virtual Drip console driver | Part of Virtual Drip console driver above. |
| 2 | `E800h` | `EBFFh` | 1024 bytes | Virtual Drip console driver | Part of Virtual Drip console driver above. |
| 3 | `EC00h` | `EFFFh` | 1024 bytes | Virtual Drip console driver | Part of Virtual Drip console driver above. |
| 4 | `F000h` | `F3FFh` | 1024 bytes | Virtual Drip console driver | Part of Virtual Drip console driver above. |
| 5 | `F680h` | `FA7Fh` | 1024 bytes | VDrip transport and storage | `F680h-F8FFh` shared transport; `F900h-FA63h` storage backend. |

## Core SIO Layout

| Range | Owner | Notes |
|---|---|---|
| `DD10h-DF4Fh` | SIO core | BIOS-owned SIO0/B async setup with CTS-polled TX and software-managed RTS helpers, legacy SIO1/A sync setup, SIO IRQ control, RX sink registration, byte I/O APIs, IO Controller RTS helpers, RX diagnostics, RX kick, ISR, and compatibility labels. |
| `DD10h-DD11h` | SIO core | Exact two-byte IM2 vector table entry. |

SIO_CH_CONSOLE is the BIOS-owned SIO0/B async console link used by the Virtual Drip console driver. RTS/CTS are connected, /DCD is tied active-low, and WR3 Auto Enables are enabled. The VDrip console driver also manages RTS through high/low watermarks for host-to-Zephyr backpressure and display-traffic gating.

## VDrip Console Layout

| Range | Owner | Notes |
|---|---|---|
| `E000h-EC9Fh` | Virtual Drip V9958 console driver | CP/M console semantics, G6 command streams, CP850 atlas upload, input queue, printable-run buffer, and sprite-cursor state. |
| `F680h-F8FFh` | Shared Virtual Drip transport | Current no-CRC frame sender, packetized readiness, single RX sink, parser, PTY dispatch, and storage reply dispatch. |

SIO_CH_IOCTRL is the legacy BIOS-owned SIO1/A synchronous helper path. Phase 1 IOCALL uses the SIO1/B Command-channel SDLC transport; SIO1/A remains inactive for Bulk in this phase.
`IOCALL` code is at `DF7Bh-DFC7h` in core BIOS.

## BIOS Jump Table Layout

| Address | Entry |
|---:|---|
| `DA00h` | `JP boot` |
| `DA03h` | `JP wboot` |
| `DA06h` | `JP const` |
| `DA09h` | `JP conin` |
| `DA0Ch` | `JP conout` |
| `DA0Fh` | `JP list` |
| `DA12h` | `JP punch` |
| `DA15h` | `JP reader` |
| `DA18h` | `JP home` |
| `DA1Bh` | `JP seldsk` |
| `DA1Eh` | `JP settrk` |
| `DA21h` | `JP setsec` |
| `DA24h` | `JP setdma` |
| `DA27h` | `JP read` |
| `DA2Ah` | `JP write` |
| `DA2Dh` | `JP listst` |
| `DA30h` | `JP sectran` |
| `DA33h` | `JP MOVE` |
| `DA36h` | `JP XMOVE` |
| `DA39h` | `JP SELMEM` |
| `DA3Ch` | `JP SETBNK` |
| `DA3Fh` | `JP IOCALL` |
| `DA42h` | `JP VIDEO_SEND` |

## Runtime State Layout

| Range | State |
|---|---|
| `FE00h` | `CURRENT_BANK` |
| `FE01h-FE02h` | `cbios_dma_addr` |
| `FE04h-FE05h` | `CONSOLE_DRIVER` |
| `FE06h-FE07h` | `CONSOLE_CALLER_SP` |
| `FE20h` | `SAVED_BANK` |
| `FE21h` | `DMA_BANK` |
| `FE22h` | `XMOVE_SRC_BANK` |
| `FE23h` | `XMOVE_DST_BANK` |
| `FE24h` | `XMOVE_PENDING` |
| `FE25h-FE26h` | `MOVE_SRC_PTR` |
| `FE27h-FE28h` | `MOVE_DST_PTR` |
| `FE29h-FE2Ah` | `MOVE_REMAIN` |
| `FE2Bh-FE2Ch` | `MOVE_CHUNK_LEN` |
| `FE40h` | `vdrip_storage_selected_drive` |
| `FE41h-FE42h` | `vdrip_storage_track` |
| `FE43h-FE44h` | `vdrip_storage_sector` |
| `FE45h` | `VDRIP_STORAGE_SAVED_BANK` |
| `FE47h` | `vdrip_storage_seq` |
| `FE48h` | `vdrip_storage_active_seq` |
| `FE49h-FE4Ah` | `vdrip_storage_lba` |
| `FE4Bh` | `vdrip_rx_mode` |
| `FE4Ch` | `vdrip_idle_mode` |
| `FE4Dh` | `vdrip_proxy_online` |
| `FE4Eh-FE4Fh` | `vdrip_raw_callback` |
| `FE50h` | `vdrip_rx_state` |
| `FE51h-FE52h` | `vdrip_declared_len` |
| `FE53h` | `vdrip_payload_len` |
| `FE54h` | `vdrip_rx_type` |
| `FE55h` | `vdrip_payload_remaining` |
| `FE56h` | `vdrip_payload_index` |
| `FE57h` | `vdrip_pending_type` |
| `FE58h` | `vdrip_pending_seq` |
| `FE59h` | `vdrip_reply_ready` |
| `FE5Ah` | `vdrip_reply_error` |
| `FE5Bh` | `vdrip_reply_status` |
| `FE68h-FE69h` | `storage_caller_sp` |
| `FE70h-FE71h` | `SIO0B_RX_SINK` |
| `FE72h-FE73h` | `SIO1_RX_SINK` |
| `FE74h` | `SIO_CORE_IRQ_ENABLED` |
| `FE75h-FE76h` | `SIO_CORE_IRQ_COUNT` |
| `FE77h` | `SIO0B_LAST_RR1` |
| `FE78h` | `SIO0B_LAST_RX_ERROR` |

## Scratch Layout

| Range | Use | Notes |
|---|---|---|
| `FA80h-FB3Fh` | `MOVE_BUFFER` | Cross-bank MOVE and VDrip storage transaction staging buffer. |
| `FB40h-FB7Fh` | `VDRIP_STORAGE_DPHDPB` | CP/M DPH/DPB constants for the VDrip storage disk. |
| `FB80h-FBFFh` | `VDRIP_STORAGE_DIRBUF` | CP/M directory buffer referenced by the VDrip storage DPH. |
| `FC00h-FCFFh` | `VDRIP_STORAGE_ALV` | CP/M allocation vector for the fixed 8 MiB VDrip work disk. |
| `FD00h-FDFFh` | unused scratch window | Derived from active buffers and scratch bounds. |

## IM2 Layout

| Field | Value | Notes |
|---|---:|---|
| Table base | `DD10h` | Start of the exact IM2 vector table entry. |
| Table range | `DD10h-DD11h` | Exactly 2 bytes; `CONSOLE_IM2_VECTOR_TABLE_END` is the exclusive end label. |
| Vector page | `DDh` | Loaded into the Z80 I register. |
| SIO0/B WR2 vector byte | `10h` | Selects the exact two-byte table entry. |
| Entry address | `DD10h` | Contains the little-endian word `sio_core_isr`. |
| Owner | SIO core | The IM2 entry lives inside core BIOS with the SIO core. |

Future devices that need additional IM2 vectors should allocate explicit table entries and program their vector bytes directly; this build no longer emits a 256-byte repeated table.

## Image Payload Layout

| Payload | Bank | Entry | Size | End | Source |
|---|---:|---:|---:|---:|---|
| *(none — firmware only, no bank payloads)* | | | | | |

## VDrip Storage Layout

| Drive | Backend | Format | Bytes | Records | Record Bytes |
|---|---|---|---:|---:|---:|
| Drive A | VDrip proxy flat image | `Zephyr VDrip CP/M 8M` | 8,388,608 | 65,536 | 128 |

## Image Artifacts

| Artifact | Size | Notes |
|---|---:|---|
| `build/firmware.bin` | 65536 bytes | Firmware image before payload attachment. |
| `build/zephyr80.pre-swap.bin` | 131072 bytes | Logical image after payload attachment and before CPU-board bit swap. |
| `build/zephyr80.bin` | 131072 bytes | Final burnable image after `tools/swapbits.py`. |

## Validation Report

Status: PASS. No fatal layout errors were found.

Validated expectations:

- BIOS core must stay inside CBIOS_CORE_BASE-CBIOS_CORE_END.
- Core BIOS component ranges must not overlap.
- Each declared driver must stay inside its declared fixed slot range.
- Virtual Drip console driver must stay inside slots 0-5.
- Shared VDrip transport must stay inside slot 5.
- VDrip storage backend must stay inside slot 5.
- SIO core and its exact IM2 vector entry must stay inside core BIOS.
- Scratch buffers and the storage allocation vector must not overlap resident code.
- Runtime state must not overlap scratch, stack, or the SIO-owned IM2 table.
- Stack guard must remain above runtime state.
- Protected/common TPA C000h-C3FFh is application-owned and must not be used by BIOS.

Additional notes:

- CP/M drive A is backed by VDrip proxy storage using a flat 8 MiB image; no RAM disk seed is embedded in ROM banks.
- Storage READ/WRITE use framed Virtual Drip transactions and preserve the standard CP/M BIOS disk call model.
- The protected TPA marker is `C000h-C3FFh`; for this build, the application-usable protected TPA subrange is `C000h-C3FFh` because `CBASE` starts at `C400h`.
- WBOOT restores the CCP range `C400h-CC05h` from ROM page 0 using `ROM_VISIBLE_BANK0` (`00h`) before returning to `CCP_CLEARBUF_ENTRY`.
- `CBIOS_BASE` is `DA00h`; CBIOS layout constants are derived from this base.
- `CBIOS_CODE_LIMIT` is `FA80h`; no resident code may cross into scratch/staging.
- SIO core code starts at `DD10h` inside core BIOS; the Virtual Drip console driver starts at `E000h`, the shared transport starts at `F680h`, and the VDrip storage backend starts at `F900h`.
- `IOCALL` code is at `DF7Bh` in core BIOS and uses the SIO1/B Command-channel SDLC transport.
- `VIDEO_SEND` code is at `DF50h` in core BIOS; raw VDP/display writes may desynchronize the V9958 logical-cell state.
- `WBOOT` resident code starts at `DADCh`, inside protected high BIOS memory.
- `ZBIOS_EXT_BASE` is at `DA33h` and exposes `MOVE`, `XMOVE`, `SELMEM`, `SETBNK`, `IOCALL`, and `VIDEO_SEND`.
- The final bit-swapped image is `build/zephyr80.bin`.
